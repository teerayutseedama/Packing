@using System.Globalization
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer localizer
@using Packing.Views.DataView;
@{
    MasterDataView masterData = (MasterDataView)ViewBag.MasterData;
}

<div class="row" style="padding-top:10px">
    <div class="form-row">
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["Plant"]</label>
            <select id="selplant" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.PlantList!.Count() > 0)
                {
                    foreach (var item in masterData.PlantList!)
                    {
                        <option value="@item.PLANT">@item.PLANT_NAME</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["MaterialCode"]</label>
            <select id="selmatcode" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.MaterialList!.Count() > 0)
                {
                    foreach (var item in masterData.MaterialList!)
                    {
                        <option value="@item.MATERIAL_CODE">@item.MATERIAL_CODE</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-2" style="padding:2px">
            <label style="font-weight:normal">@localizer["MaterialName"]</label>
            <select id="selmatname" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.MaterialList!.Count() > 0)
                {
                    foreach (var item in masterData.MaterialList!)
                    {
                        <option value="@item.MATERIAL_CODE">@item.MATERIAL_NAME</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["Shift"]</label>
            @*tbm_pk_batch_status*@
            <select id="selshift" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.ShiftList!.Count() > 0)
                {
                    foreach (var item in masterData.ShiftList!)
                    {
                        <option value="@item.ID">@item.WORK_SHIFT</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["MFGDate"]</label>
            <input id="txmfgdate" class="form-control text-center" />
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["Line"]</label>
            <select id="selline" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.LineList!.Count() > 0)
                {
                    foreach (var item in masterData.LineList!)
                    {
                        <option value="@item.PACKING_LINE_ID">@item.PK_LINE_NAME</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["Status"]</label>
            @*tbm_pk_batch_status*@
            <select id="selstatus" class="form-control select2" style="width:100%">
                <option></option>
                @if (masterData.StatausList!.Count() > 0)
                {
                    foreach (var item in masterData.StatausList!)
                    {
                        <option value="@item.ID">@item.BATCH_STATUS</option>
                    }
                }
            </select>
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["BatchNo"]</label>
            <input id="txbatch" class="form-control" />
        </div>
        <div class="form-group col-md-1" style="padding:2px">
            <label style="font-weight:normal">@localizer["RunNo"]</label>
            <input id="txrunno" class="form-control" />
        </div>
        <div class="form-group col-md-2" style="margin-bottom:0px;padding-top:27px">
            <button type="button" class="btn btn-primary" onclick="GetApprovalyList()">@localizer["Search"]</button>
            <button type="button" class="btn btn-success" id="btupdate" onclick="showmodal(1)">@localizer["Update"]</button>
            <button type="button" class="btn btn-secondary" onclick="GetExcel()">@localizer["Excel"]</button>
        </div>
        <div class="form-group col-md-12">
            <div class="form-inline">
                <table class="isr-table" id="tbl1">
                    <thead>
                        <tr>
                            <th style="width:50px">
                                <input type="checkbox" id="checkkall" onchange="checkall(this.checked)" />
                            </th>
                            <th style="width:120px">@localizer["BatchNo"]</th>
                            <th>@localizer["Plant"]</th>
                            <th>@localizer["MaterialName"]</th>
                            <th style="width:100px">@localizer["Line"]</th>
                            <th>@localizer["Package"]</th>
                            <th>@localizer["RunNo"]</th>
                            <th>@localizer["Qty"]</th>
                            <th>@localizer["UOM"]</th>
                            <th>@localizer["MFGDate"]</th>
                            <th>@localizer["ExpiredDate"]</th>
                            <th>@localizer["Status"]</th>
                            
                        </tr>
                    </thead>
                    <tbody id="approvalbody">
                     
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="modal" tabindex="-1" role="dialog" id="modalapproval">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Approval</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close" onclick="showmodal(0)">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        
            <div class="modal-body">
                <div class="row">
                    <div class="form-group col-md-12">
                        <label style="font-weight:normal">STATUS</label>
                        @*tbm_pk_batch_status*@
                        <select id="selupdatestatus" class="form-control select2" style="width:100%">
                            <option></option>
                            @if (masterData.StatausList!.Count() > 0)
                            {
                                foreach (var item in masterData.StatausList!)
                                {
                                    <option value="@item.ID">@item.BATCH_STATUS</option>
                                }
                            }
                        </select>
                    </div>
                    <div class="form-group col-md-12">
                        <label style="font-weight:normal">REMARK</label>
                        <textarea class="form-control" id="txremark" style="height:80px"></textarea>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" onclick="Approval()">Update</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal" onclick="showmodal(0)">Close</button>
            </div>
            </div>
    </div>
</div>
<script>
    batchList=[];
    $(document).ready(function () {
        SetPagename('Approval');
    });

    function showmodal(flagshow) {
        var ch = batchList.find(f=>f.checked);
        if(ch!=null){
            if (flagshow == 1) {
                $('#modalapproval').show();
            } else {
                $('#modalapproval').hide();
            }

        }else{
            Notiflix.Notify.warning('กรุณาเลือกข้อมูล');
            $('#modalapproval').hide();
        }
        
    }
    $(function () {
        var d = new Date();
        $('#txmfgdate').datepicker({
            format: 'dd/mm/yyyy',
            autoclose: true,
            todayHighlight: true,
            orientation: "auto"
        }).datepicker("setDate", d);
    });

    function GetApprovalyList() {
        batchList=[];
        document.getElementById('checkkall').checked=false;
        var txt = '';
        $.post('@Url.Action("GetApprovalDataViews", "Approval")', {
            Plant: $('#selplant').val(),
            MaterialCode: $('#selmatcode').val(),
            MaterialName: $('#selmatname').val(),
            Shift: $('#selshift').val(),
            MFGDate: moment($("#txmfgdate").val(), "DD-MM-YYYY").format("YYYY/MM/DD"),
            Line: $('#selline').val(),
            Stataus: $('#selstatus').val(),
            BatchNo: $('#txbatch').val(),
            RunNo: $('#txrunno').val()
        }, function (data) {
        if (data.length > 0) {
            for (var i = 0; i < data.length; i++) {
                var d = data[i];
                    batchList.push({ batchNo: d.batchNo, item: i + 1, checked: false, batchSub: d.batchSub });
                txt += '<tr>'+
                        '<td class="text-center" > <input type="checkbox" id ="cb1" name="cb1" class="cb1" onchange="singleCheck(this.checked,' + i + ')"/> </td>' +
                    '<td class="text-center" > ' + d.batchNo + ' </td>' +
                    '<td class="text-center" > ' + d.plant+ ' </td>' +
                    '<td> ' + d.materialName + ' </td>' +
                    '<td class="text-center" > ' + d.line+ ' </td>' +
                    '<td class="text-center" > ' + d.package + ' </td>' +
                        '<td class="text-right" > ' + d.runNo + ' </td>' +
                    '<td class="text-right" > ' + d.qty + ' </td>' +
                    '<td class="text-center" > ' + d.uom + ' </td>' +
                        '<td class="text-center" > ' + d.mfgDate + ' </td>' +
                        '<td class="text-center" > ' + d.expireDate + ' </td>' +
                        '<td class="text-center" style = "color:gold" > ' + d.stataus + ' </td>' +
                    '</tr>';
                    }
            $('#approvalbody').html(txt);
        } else {
            Notiflix.Notify.warning('ไม่พบข้อมูลที่ค้นหา');
        }
        }).fail(function () {
            Notiflix.Notify.Failure('เกิดข้อผิดพลาด ตรวจสอบข้อมูลใหม่')

        });
    }

    function GetExcel() {
        $.ajax({
            url: '@Url.Action("DownloadApprovalExcel", "Approval")',
            type: 'POST',
            data: {
                Plant: $('#selplant').val(),
                MaterialCode: $('#selmatcode').val(),
                MaterialName: $('#selmatname').val(),
                Shift: $('#selshift').val(),
                MFGDate: moment($('#txmfgdate').val(), "dd/mm/yyyy").toDate(),
                Line: $('#selline').val(),
                Stataus: $('#selstatus').val(),
                BatchNo: $('#txbatch').val(),
                RunNo: $('#txrunno').val()
            },
            responseType: 'blob', // Set the response type to blob
            success: function (response) {
                // Create a Blob from the response
                var blob = new Blob([response], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });

                // Create a link element and trigger the download
                var link = document.createElement('a');
                link.href = '/ExcelApproval/Approval.xlsx'
                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);
            },
            error: function (xhr, status, error) {
                console.error(error);
            }
        });
       
    }

    function checkall(event) {
        var chlist = document.getElementsByClassName('cb1');
        //  batchList.push({ batchNo: batchNoLast ,item:i+1,checked:false});
        for (var i = 0; i < chlist.length; i++) {
            batchList[i].checked = event;
            chlist[i].checked = event;
        }
        if (event) {

        }

    }

    function singleCheck(event, index) {
        batchList[index].checked = event;
        var ch = batchList.find(x => x.checked == false);
        var all = document.getElementById('checkkall');
        if (ch != null) {
            all.checked = false;
        } else {
            all.checked = true;
        }
    }

    function Approval(){
        var dataSave=[];
        document.getElementById('checkkall').checked = false;
        var txt = '';
        for (var i = 0; i < batchList.length; i++) {
            var d = batchList[i];
            if(d.checked){
                dataSave.push({
                    BatchNo: d.batchNo,
                    BatchSub: d.batchSub,
                    Status: $("#selupdatestatus").val(),
                    Remark: $('#txremark').val(),
                    User: localStorage.getItem('Username'),
                })
            }
        } 
        $.post('@Url.Action("SaveApprovalData", "Approval")', {
            data: dataSave
        }, function (data) {
            if(data.status){
                $('#modalapproval').hide();
                $('#approvalbody').html('');
                GetApprovalyList();
                Notiflix.Notify.Success('บันทึกข้อมูลเสร็จสิ้น');
            }else{
            Notiflix.Notify.warning(data.error);
            }
        }).fail(function () {
            Notiflix.Notify.Failure('เกิดข้อผิดพลาด ตรวจสอบข้อมูลใหม่');

        });
    }
</script>